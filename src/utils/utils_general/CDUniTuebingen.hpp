#ifndef CDUNITUEBINGEN_HPP
#define CDUNITUEBINGEN_HPP

#include <opencv2/opencv.hpp>

namespace uni_tuebingen {
namespace cd {

namespace primary {
static const cv::Scalar karmin_red = cv::Scalar(55, 30, 165);
static const cv::Scalar gold = cv::Scalar(105, 160, 180);
static const cv::Scalar anthrazite = cv::Scalar(75, 65, 50);
}

namespace secondary {
static const cv::Scalar dark_blue = cv::Scalar(140, 90, 65);
static const cv::Scalar blue = cv::Scalar(170, 105, 0);
static const cv::Scalar cyan = cv::Scalar(200, 170, 80);
static const cv::Scalar light_green = cv::Scalar(160, 185, 160);
static const cv::Scalar green = cv::Scalar(75, 165, 125);
static const cv::Scalar dark_green = cv::Scalar(30, 110, 50);
static const cv::Scalar red = cv::Scalar(60, 80, 200);
static const cv::Scalar lila = cv::Scalar(150, 110, 175);
static const cv::Scalar light_brown = cv::Scalar(150, 160, 180);
static const cv::Scalar beige = cv::Scalar(105, 180, 215);
static const cv::Scalar orange = cv::Scalar(0, 150, 210);
static const cv::Scalar brown = cv::Scalar(70, 105, 145);
}
}
}

#endif // CDUNITUEBINGEN_HPP
